<p> will this appear in contractFunctions </p>
<div for="contract in data.contractMeta">
  <div for="(symbol,symbolObj) in contract.xabi.funcs">
      <b>${symbol}</b>
        <div for="(key,val) in symbolObj">
            <b>${key}</b>: ${val}
        </div> 
  </div>
</div>


